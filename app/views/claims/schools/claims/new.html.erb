<% content_for :page_title, t(".page_title") %>
<% render "claims/schools/primary_navigation", school: @school, current: :claims %>

<%= content_for(:before_content) do %>
  <%= govuk_back_link(href: claims_school_claims_path(@school)) %>
<% end %>

<div class="govuk-width-container">
  <%= render(
    partial: "form",
    locals: {
      claim_provider_form:,
      form_url: claims_school_claims_path(@school),
      method: :post,
      school: @school,
    },
  ) %>
</div>
