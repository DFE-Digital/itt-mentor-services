<% render "claims/schools/primary_navigation", school: @school, current: :claims %>

<div class="govuk-width-container">
  <%= render(
    partial: "#{step}_form",
    locals: { claim:, mentor_trainings:, school: },
  ) %>

  <p class="govuk-body">
    <%= govuk_link_to(t(".cancel"), claims_school_claims_path, no_visited_state: true) %>
  </p>
</div>
