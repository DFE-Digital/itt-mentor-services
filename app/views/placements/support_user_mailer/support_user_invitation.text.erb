<%= @user_name %>,

You have been invited to join the <%= @service_name %> service as a support user.

[Sign in to the support console for <%= @service_name %>](<%= @sign_in_url %>)

If you need help [contact the service team on Slack](<%= @slack_url %>).

<%= @service_name %> service
