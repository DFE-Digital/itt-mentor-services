<%= render "placements/support/primary_navigation", current_navigation: :users %>

<%= content_for(:before_content) do %>
  <%= govuk_back_link(href: back_link_path) %>
<% end %>

<div class="govuk-width-container">
  <%= render @wizard.step, object: @wizard.step %>

  <p class="govuk-body">
    <%= govuk_link_to(t(".cancel"), placements_support_support_users_path, no_visited_state: true) %>
  </p>
</div>
