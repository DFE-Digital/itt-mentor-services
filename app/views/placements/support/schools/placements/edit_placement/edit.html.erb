<%= render "placements/support/primary_navigation", current_navigation: :organisations %>

<%= content_for(:before_content) do %>
  <%= govuk_back_link(href: back_link_path) %>
<% end %>

<div class="govuk-width-container">
  <%= render @wizard.step, object: @wizard.step, contextual_text: t(".edit_placement", organisation_name: @school.name) %>

  <p class="govuk-body">
    <%= govuk_link_to(t(".cancel"), placements_support_school_placement_path(@school, @placement), no_visited_state: true) %>
  </p>
</div>
