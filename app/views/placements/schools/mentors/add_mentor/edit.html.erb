<%= render "placements/schools/primary_navigation", school: @wizard.school, current_navigation: :mentors %>

<%= content_for(:before_content) do %>
  <%= govuk_back_link(href: back_link_path) %>
<% end %>

<div class="govuk-width-container">
  <%= render @wizard.step, object: @wizard.step %>

  <p class="govuk-body">
    <%= govuk_link_to(t(".cancel"), placements_school_mentors_path(@school), no_visited_state: true) %>
  </p>
</div>
