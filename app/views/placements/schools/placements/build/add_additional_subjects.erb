<% content_for :page_title, @placement.errors.any? ? t(".title_with_error") : t(".title") %>
<%= render "placements/schools/primary_navigation", school: @school, current_navigation: :placements %>

<%= content_for(:before_content) do %>
  <%= govuk_back_link(href: @previous_step) %>
<% end %>

<div class="govuk-width-container">
  <%= form_for(@placement, url: placements_school_placement_build_path(@school, id: :add_additional_subjects), method: :put) do |f| %>
    <%= f.govuk_error_summary %>

    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        <span class="govuk-caption-l"><%= t(".add_placement") %></span>

        <%= f.govuk_check_boxes_fieldset :additional_subject_ids, legend: { size: "l", text: @selected_subject.name } do %>
          <% @additional_subjects.each do |additional_subject| %>
            <%= f.govuk_check_box :additional_subject_ids, additional_subject.id, label: { text: additional_subject.name },
                                     checked: @selected_additional_subjects&.include?(additional_subject) %>
          <% end %>
        <% end %>

        <%= f.govuk_submit t(".continue") %>
      </div>
    </div>
  <% end %>

  <p class="govuk-body">
    <%= govuk_link_to(t(".cancel"), placements_school_placements_path(@school), no_visited_state: true) %>
  </p>
</div>
