<%= render AutocompleteSelectFormComponent.new(
  model: provider_step,
  scope: provider_step.scope,
  url: current_step_path,
  method: :put,
  data: {
    autocomplete_path_value: "/api/provider_suggestions",
    autocomplete_return_attributes_value: %w[code],
  },
  input: {
    value: provider_step.name,
    label: t(".title"),
    caption: t(".caption"),
    previous_search: provider_step.id,
  },
) %>
